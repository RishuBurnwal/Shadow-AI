// script.js — app-level entry script loaded before renderer.js
// Provides any pre-initialization that needs to happen before the renderer loads.
// Currently a placeholder; the renderer.js handles all core app logic.
console.log('Shadow AI app initializing...');
